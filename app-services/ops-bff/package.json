{
  "name": "@fleetfeast/ops-bff",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/**/*.test.js",
    "start": "npm run build && node dist/main.js"
  },
  "dependencies": {
    "@fleetfeast/app-auth": "0.1.0",
    "@fleetfeast/shared-contracts": "0.1.0",
    "fastify": "^5.2.1"
  },
  "devDependencies": {
    "@types/node": "^24.10.13"
  }
}
